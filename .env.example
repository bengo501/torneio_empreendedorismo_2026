# copie para .env na raiz (não commitar)

# backend — chave sympla (preferir SYMPLA_TOKEN em vez de VITE_*)
SYMPLA_TOKEN=
PORT=3001

# frontend — deixe vazio em dev (vite faz proxy para :3001)
# em preview remoto, aponte para a url do backend se estiver separado
VITE_API_URL=
